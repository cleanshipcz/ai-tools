id: global-code-style
description: Personal formatting preferences that apply to every tool and every project
rules:
  - "Never manually wrap code, comments, strings, Markdown, JSON, YAML, or shell commands at any column width. One logical statement per line; rely on editor soft-wrap."
metadata:
  version: 1.0.0
  author: cleanshipcz
  created: "2026-08-10"
  tags:
    - global
    - formatting
